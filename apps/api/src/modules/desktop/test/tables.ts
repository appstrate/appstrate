// SPDX-License-Identifier: Apache-2.0

/**
 * The desktop module owns no tables — connected clients live in an
 * in-memory registry. The root test preload auto-discovers this file;
 * an empty list keeps the contract explicit.
 */
export default [] as const;
