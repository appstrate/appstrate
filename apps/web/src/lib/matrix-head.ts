// SPDX-License-Identifier: Apache-2.0

/**
 * The head band of a MATRIX (roles × permissions, org roles × capabilities),
 * written like `DataTable`'s head so a matrix reads as the same family while
 * keeping the raw table a matrix needs: it scrolls where a list degrades.
 */
export const MATRIX_HEAD =
  "text-muted-foreground h-10 text-[0.68rem] font-semibold tracking-[0.05em] uppercase";
