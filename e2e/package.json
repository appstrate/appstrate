{
  "name": "@appstrate/e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bunx --bun playwright test",
    "test:api": "bunx --bun playwright test --project=api",
    "test:ui": "bunx --bun playwright test --project=ui",
    "test:ui-open": "bunx --bun playwright test --project=ui --ui",
    "report": "bunx --bun playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0"
  }
}
