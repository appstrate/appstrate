# Generated files — managed by scripts, not prettier
packages/core/schema/

# Pricing catalog — generated by scripts/refresh-pricing-catalog.ts
# (litellm sync). Written with JSON.stringify(,,2); prettier would
# collapse the arrays and fight the generator on every refresh.
apps/api/src/data/pricing/

# Drizzle SQL migrations — managed by drizzle-kit, not prettier
**/drizzle/migrations/**

# Bun config files — toml, no prettier parser
**/bunfig.toml

# AFPS example prompt sources — Mustache whitespace is semantic inside
# sections; prettier's markdown reformat injects blank lines that leak
# into rendered output.
packages/afps-runtime/examples/**/source/*.md
packages/afps-runtime/fixtures/
