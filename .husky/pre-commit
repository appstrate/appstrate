# Fail on the first failing step. Husky's runner already invokes this file as
# `sh -e` (.husky/_/h), but that file is regenerated by `husky` on every install
# and is not ours to depend on — and `set -e` also covers any line appended
# below. Measured 2026-09-07: with a failing first line, `sh .husky/pre-commit`
# exits 0 without it and 1 with it.
set -e
bunx lint-staged
bun scripts/hooks/secret-scan.ts
