node_modules
# `/app` extracted from the OSS image so `@appstrate/core` resolves with bun's
# store intact — see the `test` job in .github/workflows/check.yml.
.oss-image
dist
*.tgz
.turbo
.DS_Store
.env
docker-compose.override.yml
