{
  "name": "@afps/output",
  "version": "1.0.0",
  "type": "tool",
  "schemaVersion": "1.3",
  "displayName": "Output",
  "description": "Emit structured output values from an agent run — emits `output.emitted` events.",
  "license": "Apache-2.0",
  "entrypoint": "src/index.ts",
  "tool": {
    "name": "output",
    "description": "Emit a structured output value. Object fields are deep-merged with prior output events (JSON merge-patch); arrays and scalars replace wholesale.",
    "inputSchema": {
      "type": "object",
      "required": ["data"],
      "additionalProperties": false,
      "properties": {
        "data": {
          "description": "Structured output — objects are merge-patched."
        }
      }
    }
  }
}
