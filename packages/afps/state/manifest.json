{
  "name": "@afps/state",
  "version": "1.0.0",
  "type": "tool",
  "schemaVersion": "1.3",
  "displayName": "State",
  "description": "Persist carry-over state between agent runs — emits `state.set` events.",
  "license": "Apache-2.0",
  "entrypoint": "src/index.ts",
  "tool": {
    "name": "set_state",
    "description": "Overwrite the agent's carry-over state for the next run. Last-write-wins; the most recent call fully replaces any previous state.",
    "inputSchema": {
      "type": "object",
      "required": ["state"],
      "additionalProperties": false,
      "properties": {
        "state": {
          "description": "Arbitrary JSON value stored as carry-over state."
        }
      }
    }
  }
}
