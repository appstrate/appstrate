# @afps/memory

AFPS platform tool — durable memories across agent runs.

Emits `memory.added` events. Storage is the EventSink's responsibility.

```ts
import tool from "@afps/memory";
// register `tool` with your AFPS runtime
```

See the [AFPS spec](https://afps.dev/spec.md) §8 for the normative interface contract.
