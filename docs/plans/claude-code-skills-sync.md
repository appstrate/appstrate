<!-- SPDX-License-Identifier: Apache-2.0 -->

# Claude Code skills sync — decisions

Goal: a user installs one Claude Code plugin and, from then on, every skill of
the spaces they reach in their pinned Appstrate organization is
available locally as `/appstrate:<skill>`, kept up to date without a manual
step. The connected plugin also supplies the organization's MCP endpoint, and
every agent active in the pinned space as `/appstrate:run-<agent>`, a generated
command that launches it through that endpoint (D18–D25). The same sync feeds
OpenAI Codex skills; Codex MCP is configured separately.

**How it behaves is documented in `apps/cli/README.md` → `appstrate packages sync`**
— flags, targets, failure modes, ownership rules, the marketplace command
string, and the cron / `launchd` fallback. This file records only the choices
behind it and what is still open.

The mechanism is entirely client-side: Claude Code plugin marketplaces accept
`source: "command"`, a locally installed tool prints the path of a directory
holding a complete plugin, and Claude Code re-runs the command once per session
in the background and installing a new copy when the directory's content hash
changes. A fresh session applies context and MCP changes reliably. No daemon
or server change.

## Decisions

| #   | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Rejected                                                                                      | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | Published `latest` by default; `--source draft` opt-in                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Draft by default                                                                              | Immutable, integrity-checked artifacts are what runs pin and what every skill channel ships. Authors iterating locally get `--source draft`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D2  | `mode: "copy"`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `mode: "link"`                                                                                | Only copy versions by file CONTENT, so a changed skill reloads mid-session. Link needs a new path to signal change and is refused on Windows                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D3  | `source: "command"` running the CLI                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | A plugin `SessionStart` hook                                                                  | The command source is the documented, consented, once-per-session mechanism with in-session reload; a hook fires on the cached copy                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| D4  | Skill directory = the frontmatter `name` when it is a legal Agent Skills name, else the slugified package `name` segment. Collision → `<scope>-<name>`, `-2`, …; an installed name stays with its package while the package is still wanted (listed, resolved or not): the holder takes its preferred name when free, else keeps its installed one. A name changes owner only when its holder leaves or renames itself (a new frontmatter `name`); the freed name may go to another package at a later sync. Newcomers take the fallback. When the targets' ledgers disagree, the first target in `SYNC_TARGETS` order decides | The package id `@scope/name` as directory; reassigning names by package-id order on every run | The spec requires dir == `name`, and `@scope/name` is not a legal skill name. A slash command switches to a different package only after its holder left or renamed itself, never because of one run's resolution order. Every rename is reported on stderr                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D5  | The sync rewrites the `name` line and nothing else                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Injecting a description; rejecting the skill                                                  | PR #1252 made the platform refuse to publish a non-conforming `SKILL.md`. Legacy artifacts still sync as authored, with a note (see D5b)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D5b | A materialized `SKILL.md` failing `checkSkillMarkdown` is one stderr note                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Failing the skill, or staying silent                                                          | Same rule as the platform, one definition (`@appstrate/afps-shared/companion-files`). Inventing content would hide the fix from its author                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| D6b | Before the CLI is connected, `--print-path` installs a setup plugin — one skill + a `SessionStart` hook that surfaces the remedy (fresh plugin only); the marketplace command falls back to `npx` when the CLI is absent                                                                                                                                                                                                                                                                                                                                                                                                       | Failing the install with a remedy under `/plugin → Errors`                                    | Nobody opens that screen. A skill the model can read is where the remedy gets acted on; an existing plugin is never replaced by it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| D6  | Deterministic output: no timestamps, no sync metadata, sorted entries, no `version` in `plugin.json`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Stamping a sync date                                                                          | The copy-mode hash IS the version; a byte-identical re-run must count as up to date                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| D7  | Ownership ledger outside the plugin tree (`<data>/skills-sync/state.json`), recorded with the root it was written under                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Re-download everything each run                                                               | Enables the cheap diff and the only safe deletion rule for the shared `~/.agents/skills/`; the root guards against a redirected `HOME`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| D8  | Targets `claude-plugin` (default), `codex`, `claude-user`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Claude plugin only                                                                            | Same materializer, different destination. `codex` is the only Codex path; `claude-user` covers `disableCommandPluginSources` and cron setups                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D9  | One installation per destination = the currently synchronized profile context, which is `profileName` + `instance` + `userId` + `orgId` — four fields, the pinned space NOT among them                                                                                                                                                                                                                                                                                                                                                                                                                                         | One plugin per profile / org; counting the pinned space as part of that identity              | The marketplace entry is a static string; multi-org users switch with `appstrate org switch` or run `--profile` from cron. D14 took the pin out of skill selection, so `appstrate space switch` installs the very same skills: the pin is content — `.mcp.json` (D12) and the agent commands (D19) — compared BY CONTENT and rewritten when it differs. Counting it as identity bought nothing and cost the all-or-nothing rule — one package that persistently fails to resolve blocked a header rewrite. A pin that moves DURING a run still aborts the swap, because the `.mcp.json` bytes were computed from it before the downloads started                                          |
| D10 | Marketplace = the dedicated public repo `appstrate/claude-plugins`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `.claude-plugin/` inside this monorepo                                                        | Adding a marketplace clones the repo; nobody should clone the platform monorepo for a 1 KB JSON                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| D11 | No server change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | A bulk `GET /api/packages/skills/bundle`                                                      | One list + N small JSON calls + downloads-on-change is fine for tens of skills; add the bulk route only if measured                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| D12 | Connected plugin includes a deterministic root `.mcp.json`, derived only from the profile's instance, orgId and spaceId (`X-Space-Id` header); setup plugin has none                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Skills only; copying CLI tokens; MCP on the org default space                                 | Issue #1261: the existing HTTP endpoint uses browser OAuth independently of the CLI. The header keeps operations in the active space; selecting additional skill sources must not change the MCP execution context                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| D13 | Codex MCP setup is documented through `codex mcp add` / `login`; sync continues to write skills only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | A new CLI wrapper or editing Codex configuration during sync                                  | The v1 documentation alternative in #1261 preserves existing client settings and avoids owning a second client's configuration lifecycle                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D14 | Skill sources default to every space this profile is a MEMBER of with `skills:read` there — the rows `GET /api/spaces` returns with `access: "member"`, not every row it returns; `--space` and `syncSpaces` only narrow that set                                                                                                                                                                                                                                                                                                                                                                                              | The pinned space alone; an opt-in list as the only way to widen                               | Being granted a space is what should put its skills on the machine, and losing one is what should take them off. A manual second step makes the grant not mean anything, and leaves revoked skills installed until someone edits a TOML file. `listSpacesForPrincipal` already answers per principal, so the grant is the whole mechanism — but it answers what the caller may SEE, which is wider: it also lists a `closed` space an org member has not joined so they can ask, and naming one in `X-Space-Id` is refused 403 `not_a_space_member`, which would fail the whole sync. `access` and `permissions` travel on every row, so the membership is read where the list already is |
| D15 | A configured `syncSpaces` id this profile can no longer use — unlisted, or listed without membership / `skills:read` — is dropped with a note naming which; an explicit `--space` that resolves to nothing, or to a space it cannot use, fails the run                                                                                                                                                                                                                                                                                                                                                                         | Failing the run on any unreachable configured id                                              | Losing access is a decision made elsewhere, not a fault of this run, and it must not stop the spaces that ARE reachable from syncing. A `--space` flag was typed just now, so it gets immediate feedback instead                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| D16 | Sync installs the org-level `latest` of each package, not the version `space_packages.version_id` pins in that space                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Resolving the space's pinned version                                                          | A package is an org row that `space_packages` attributes to spaces, and `versions/latest` resolves by dist-tag with no space argument. With several spaces a package can be pinned differently in each, and one machine holds one copy — so the org's `latest` is the only answer that does not depend on which space happened to list it first                                                                                                                                                                                                                                                                                                                                           |
| D17 | Sync warns when the pinned space is not one this profile is a member of — absent from `GET /api/spaces`, or listed with `access: "none"` — and syncs the usable spaces anyway                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Failing the run; falling `.mcp.json` back to another space                                    | D14 removed the pin from skill selection, so a dead pin no longer surfaces as a failed listing — but D12 still sends it as `X-Space-Id`, and the server refuses a header naming a space the principal is not a member of — being listed changes nothing about that refusal. Skills would be current while MCP silently 403s. The run has the space list in hand, so the note costs nothing; changing the MCP space on the user's behalf would move their execution context without asking                                                                                                                                                                                                 |
| D18 | An agent is one more generated skill — `skills/run-<name>/SKILL.md` + `input.json` — written to the `claude-plugin` target only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Writing it to `codex` / `claude-user` too                                                     | Only the plugin ships `.mcp.json` (D12); anywhere else `run_and_wait` does not exist and the command fails every time                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| D19 | Agents come from the pinned space only, whatever `--space` / `syncSpaces` select; skills stay multi-space (D14). Listed from `GET /api/agents` (the space's launchable set). The profile needs membership and core's `agentCapabilities(has, has("mcp:invoke"))` reaching `run` there — `agents:run` plus `runs:read` or `runs:read-all` plus `mcp:invoke` (contributed by the MCP module), exactly the grant the MCP server exposes `run_and_wait` and `invoke_operation` on — else no agent command and a note on every sync. System agents active in the space are included                                                 | `space_id` on `run_and_wait`; one MCP server per space in `.mcp.json`                         | An MCP session is one space: permissions, tool surface, operation index and uploads resolve against `X-Space-Id`, and an agent not active there is refused `404`. The first is a special case that still breaks file inputs, because uploads are space-scoped; the second costs N OAuth connections and N operation indexes in every session. A skill is content, an agent is an execution: the asymmetry is the model                                                                                                                                                                                                                                                                    |
| D20 | The command carries the space's launch contract: `partitionInputFields` (`@appstrate/core/input-resolution`, the dashboard launch form's rule) splits the top-level fields into locked / prefilled / prompted. Stored values are never written to disk — only which fields have one                                                                                                                                                                                                                                                                                                                                            | Inlining the raw manifest schema; writing the stored values                                   | The first produces `400 locked_input_field`; the server itself flags stored values as often an account id or a credential. One rule shared with the dashboard keeps the form and the command from disagreeing                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| D21 | Data and instructions are separate. The `SKILL.md` body holds generator text plus regex-validated identifiers (package, version); the space id lives only in `metadata.appstrate-space`, every org-authored string in `input.json`, and the description in a JSON-escaped frontmatter scalar. Every tool is named in full (`mcp__plugin_appstrate_appstrate__…`), since another Appstrate MCP server can expose the same bare names                                                                                                                                                                                            | Inlining schema, description or field names in the body                                       | Claude Code preprocesses a skill body (`$ARGUMENTS`, `${…}`, injected shell commands): org text there would hand an agent author shell execution on the user's machine. Escaping keeps a description from injecting `allowed-tools:`                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D22 | Version pinned in the command — `--source published` → the version `latest` resolves to, `--source draft` → `draft`; an agent the user cannot write (`draft_not_writable`) is reported and removed, like a skill — refused again on every run, it is not kept as a transient failure and does not block a context switch; a system agent always resolves published. Ledger `integrity` = SRI of the generated tree (`treeIntegrity`)                                                                                                                                                                                           | Hashing the agent artifact; bumping `STATE_VERSION`                                           | A version, schema, lock, template or pinned-space change moves the hash and rewrites the command by itself; nothing is downloaded for agents; the ledger format is unchanged                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D23 | Slugs `run-<name>`, collision → `run-<scope>-<name>`, then `-2`, `-3`, …; skills are assigned first, and D4's stability rule holds: an installed command keeps its name while its agent is wanted; every rename reported                                                                                                                                                                                                                                                                                                                                                                                                       | Separate namespaces                                                                           | Commands share one flat `/appstrate:` namespace; an agent never renames an installed skill (a brand-new skill still falls back when an installed agent command holds its name); the prefix says "this launches a metered run"; a command whose name moved to another agent would bill that agent                                                                                                                                                                                                                                                                                                                                                                                          |
| D24 | `disable-model-invocation: true`; no `allowed-tools` — neither reading `input.json` nor `run_and_wait` is pre-approved. The body never relaunches once a run id was returned                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Pre-approving the run; pre-approving `Read(${CLAUDE_SKILL_DIR}/input.json)`                   | The permission prompt shows the `input`: unless the user allowed the tool permanently or runs in an auto-accept mode, it is the confirmation before a metered run. Claude Code's docs do not show `${CLAUDE_SKILL_DIR}` substituted in a `Read` rule, so that pre-approval was unverifiable. Relaunching after a run id would start a second metered run. Without model invocation the description also stays out of the model's context, so N agent commands cost nothing until invoked                                                                                                                                                                                                  |
| D25 | File fields: `createUpload` through `invoke_operation` (`describe_operation` read for its body shape only) → `PUT` the raw bytes with the returned headers → `upload://` URI; or an `appfile://` URI from `list_files`                                                                                                                                                                                                                                                                                                                                                                                                         | Inline `data:` URIs                                                                           | Base64 would go through model tokens; upload and run share the pinned space (D19)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

Writing and comparing the connected plugin use the same expected fixed files.
An instance, org or space change therefore updates `.mcp.json` even when every
skill is unchanged. Skill sources default to every space this profile is a member of with
`skills:read` there; repeated `--space` flags, then profile `syncSpaces`,
narrow that set. Agent commands ignore that selection: they come from the
pinned space alone (D19), so a space change replaces them too. A command the
generator refuses to render (a server value that fails its identifier checks)
is reported and removed, not kept: unlike a transient read error, the next run
would fail the same way. The
listings run concurrently but merge in selection order, so a package attributed
to several spaces is downloaded once, from the first that listed it — and which
one that is never changes the version, which `versions/latest` resolves per org.
Explicit flags accept IDs or unambiguous exact names. Persistent selections
contain space IDs and are cleared when the organization changes. An empty
`syncSpaces` list installs no business skills and retains MCP. The server
validates `X-Space-Id` against the org (`resolveMcpSpaceRow`) and forwards it
on every in-process dispatch, so the header cannot reach another org's space.
The CLI reference documents OAuth, reloads, legacy connection coexistence and
opt-out.

## Active context and removal

There is one managed context per destination, not one accumulated installation
per profile. A successful context switch replaces the old installation. The
plugin's skills, agent commands and generated MCP connection are prepared and
replaced together; a failed replacement preserves the previous plugin. The MCP
header always uses the pinned active space, even when skills are selected from
several spaces — and the pin is not part of the context (D9): switching space
rewrites `.mcp.json` and the agent commands and keeps every installed skill,
without the all-or-nothing rule a context switch carries. A pin that moves
mid-run still aborts the swap.

Explicit logout removes the profile's managed shared skills and replaces its
generated plugin with setup files, without `.mcp.json` or agent commands.
Another profile's targets are preserved. Logout still removes local
credentials when remote revocation or skill cleanup fails; cleanup failures
retain ownership for a later retry. The same sync lock serializes cleanup and
installation. A ledger names its context, and one that does not is not this
format: it is refused whole rather than read as "owner unknown", so no code
path has to decide whose directories those are.
The next sync re-materializes and owns them again.

Cleanup changes the generated source immediately. Claude's installed cache and
an active session are separate: the next command-source refresh, or
`claude plugin update appstrate@appstrate` followed by a fresh session, applies
the new copy. Removing the default CLI profile may activate another configured
profile; that next sync installs the newly active profile instead of setup.
Without a configured profile, it keeps setup. These operations distribute
skills and agent commands; they do not introduce package authoring or bidirectional folders.

Codex supports plugins and plugin-provided MCP servers. Its current documented
plugin installation does not provide the Claude marketplace command-source
sync mechanism used here; direct skill directories plus explicit MCP setup
keep this implementation small. See [building Codex plugins](https://learn.chatgpt.com/docs/build-plugins)
and [Codex MCP](https://learn.chatgpt.com/docs/extend/mcp?surface=cli).

## Still open

- **Release**: tag `cli@<version>` after merge. The marketplace command is
  `appstrate packages sync …` since the command moved under `packages`; the
  companion `appstrate/claude-plugins` change is merged only once the CLI
  release that carries the new command is on npm, and each machine re-accepts
  the changed command once (`claude plugin update appstrate@appstrate`).

## Follow-ups (not in this plan)

- **Integration test** (label `integration`, real API via `getTestApp`):
  publish two skills, sync, assert the tree; publish a new version, sync,
  assert only that one re-downloaded. Every server interaction is covered today
  by a stub built from real `.afps` archives
  (`apps/cli/test/helpers/skills-server.ts`); what is missing is the round-trip
  against the real routes.
- **Bulk endpoint**: extend the skills list DTO with `latest_version` +
  `latest_integrity` (one LEFT JOIN on the `latest` dist-tag) to drop the N
  version calls. Server change + OpenAPI + `generate:api`.
- **Codex MCP automation**: consider a wrapper only if the documented setup
  proves insufficient. It would need an explicit ownership and conflict policy
  for existing entries, org switches and client failures before it could write
  client configuration safely.
- **Push freshness**: a plugin `monitor` polling `GET /api/packages/skills` and
  `GET /api/packages/agents` and re-running the sync — only if once-per-session
  proves too slow in practice.
- **Package events** on the webhook module (`package.version_created`) — would
  benefit any external sync, not just this one.
- **Multi-space agents**: agent commands from every space the profile reaches.
  The right shape is a multi-space MCP session — the space an argument of every
  tool, the tool surface computed per call — an issue of its own, not a
  `space_id` argument on `run_and_wait` alone (D19).
- **Codex agent commands**: need an MCP server on the Codex side first (D13,
  D18).
